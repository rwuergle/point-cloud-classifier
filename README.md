# Point Cloud Classifier
This repository presents a method to classify point clouds generated by *aerial laser scanning*. 
## Quick Links
* [Documentation](docs/README.md)
* [Installation](#installation)

## Installation

Clone the repository and install the package locally from the project root.

```bash
git clone https://github.com/rwuergle/point-cloud-classifier.git
cd point-cloud-classifier
python -m venv .venv
.\.venv\Scripts\activate
pip install --upgrade pip
pip install -r requirements.txt
pip install -e .
```

If you prefer a normal install instead of editable mode:

```bash
pip install .
```

If you want to install the full development environment from `requirements.txt`:

```bash
pip install -r requirements.txt
```

## Usage

```python
from point_cloud_classifier.classifier import PointCloudClassifier

classifier = PointCloudClassifier()

# Example: use classifier methods after loading or computing point cloud features
# points, X = ...  # build or load input feature arrays first
# labels = classifier.classify_ground_points(X)
```

## License

This project is licensed under the [MIT License](LICENSE)